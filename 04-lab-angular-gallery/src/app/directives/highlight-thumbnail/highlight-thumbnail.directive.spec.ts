import { HighlightThumbnailDirective } from '../highlight-thumbnail.directive';

describe('HighlightThumbnailDirective', () => {
	it('should create an instance', () => {
		const directive = new HighlightThumbnailDirective();
		expect(directive).toBeTruthy();
	});
});
